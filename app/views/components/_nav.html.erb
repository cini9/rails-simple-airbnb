<div class="nav">
  <%= link_to "Simple Airbnb", flats_path, class: "home" %>
  <div class="add-search d-flex">
    <%= form_tag flats_path, method: 'get', class: 'navbar-form d-flex' do %>
      <div class="input-group">
        <%= text_field_tag :query, params[:query], size: 50, placeholder: "Search", class: "form-control mr-1"%>
      </div>
      <%= submit_tag 'Search', class: 'btn btn-success mr-1' %>
    <% end %>
    <%= link_to "Add Flat", new_flat_path, class: "btn btn-outline-primary" %>
  </div>
</div>
